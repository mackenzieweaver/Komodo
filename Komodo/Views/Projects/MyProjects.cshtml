@model IEnumerable<Komodo.Models.Project>

@{
    ViewData["Title"] = "My Projects";
}

<h1>My Projects</h1>
<p>
    <a class="btn btn-outline-dark" asp-controller="Projects" asp-action="Create">New Project</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.ImagePath)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImageData)
            </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                @*<td>
                    @Html.DisplayFor(modelItem => item.ImagePath)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ImageData)
                </td>*@
            <td>
                <a asp-controller="Projects" asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-controller="Projects" asp-action="Details" asp-route-id="@item.Id">Details</a>
                <span> | </span><a asp-action="AssignUsers" asp-route-id="@item.Id">Assign</a>
            </td>
            </tr>
        }
    </tbody>
</table>
